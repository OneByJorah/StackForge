# Honcho self-hosted LLM provider configuration
# Copy to .env.honcho and fill in your API keys
# Any OpenAI-compatible provider works (OpenRouter, Together, etc.)

# Primary LLM provider
LLM_VLLM_API_KEY=your-primary-api-key
LLM_VLLM_BASE_URL=https://openrouter.ai/api/v1

# Embeddings — independent of LLM providers
LLM_EMBEDDING_API_KEY=your-embedding-api-key
LLM_EMBEDDING_BASE_URL=https://openrouter.ai/api/v1
LLM_EMBEDDING_MODEL=openai/text-embedding-3-small

# Backup LLM provider (leave empty to disable)
LLM_OPENAI_COMPATIBLE_API_KEY=

# Needed for client initialization (can be same as primary)
LLM_OPENAI_API_KEY=your-primary-api-key